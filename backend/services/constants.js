// salt rounds for hashing
const SALT_ROUNDS = 10;

// preffered watch_providers and countries for certifications
const preferredProviders = [8, 9, 2, 10, 337, 531, 15, 350, 1968, 386, 1770, 1899];
const preferredCountries = ['AU', 'US', 'GB', 'IN'];


module.exports = { preferredProviders, preferredCountries, SALT_ROUNDS };
